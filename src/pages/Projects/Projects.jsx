import styles from './Projects.module.css';

const Projects = () => {
  return (
    <div 
      className={`container ${styles.projects}`}
      id='projects'>
      projects
    </div>
  )
}

export default Projects;
