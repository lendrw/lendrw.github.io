import styles from './Contact.module.css';

const Contact = () => {
  return (
    <div 
      className={`container ${styles.contact}`}
      id='contact'>
      contact
    </div>
  )
}

export default Contact;
